<odoo>
  <template id="helpdesk_form_page" name="Formulario Helpdesk" website="True">
    <t t-call="website.layout">
      <div class="container mt16 mb16">
        <h1>Abrir un Ticket de Soporte</h1>
        <form action="/helpdesk/submit" method="post" enctype="multipart/form-data">
          <div class="form-group">
            <label for="name">Nombre Completo</label>
            <input type="text" name="name" id="name" required class="form-control"/>
          </div>
          <div class="form-group">
            <label for="email">Correo Electrónico</label>
            <input type="email" name="email" id="email" required class="form-control"/>
          </div>
          <div class="form-group">
            <label for="phone">Teléfono</label>
            <input type="text" name="phone" id="phone" class="form-control"/>
          </div>
          <div class="form-group">
            <label for="subject">Asunto</label>
            <input type="text" name="subject" id="subject" required class="form-control"/>
          </div>
          <div class="form-group">
            <label for="category_id">Categoría</label>
            <select name="category_id" id="category_id" required class="form-control">
              <option t-foreach="request.env['helpdesk.category'].sudo().search([])" 
                      t-as="cat" 
                      t-att-value="cat.id">
                <t t-esc="cat.name"/>
              </option>
            </select>
          </div>
          <div class="form-group">
  <label for="service_id">Servicio</label>
  <select id="service_id" name="service_id" required class="form-control">
    <option value="">Seleccione Servicio</option>
  </select>
</div>
          <div class="form-group">
            <label for="priority">Prioridad</label>
            <select name="priority" id="priority" class="form-control">
              <option value="0">Baja</option>
              <option value="1" selected>Normal</option>
              <option value="2">Alta</option>
              <option value="3">Urgente</option>
            </select>
          </div>
          <div class="form-group">
            <label for="description">Descripción</label>
            <textarea name="description" id="description" rows="6" class="form-control" required></textarea>
          </div>
            <input type="file" name="attachment" id="attachment" multiple/>
          <button type="submit" class="btn btn-primary">Enviar Ticket</button>
        </form>
      </div>
    </t>
  </template>

  <template id="thank_you" name="Gracias">
    <t t-call="website.layout">
      <div class="container mt16 mb16 text-center">
        <h2>¡Gracias!</h2>
        <p>Tu ticket <strong><t t-esc="ticket.name"/></strong> ha sido recibido.</p>
      </div>
    </t>
  </template>
</odoo>
