<odoo>
  <record id="view_helpdesk_ticket_tree" model="ir.ui.view">
    <field name="name">helpdesk.ticket.tree</field>
    <field name="model">helpdesk.ticket</field>
    <field name="arch" type="xml">
      <tree>
        <field name="name"/>
        <field name="partner_id"/>
        <field name="priority"/>
        <field name="stage_id"/>
        <field name="team_id"/>
        <field name="create_date"/>
      </tree>
    </field>
  </record>

  <record id="view_helpdesk_ticket_form" model="ir.ui.view">
    <field name="name">helpdesk.ticket.form</field>
    <field name="model">helpdesk.ticket</field>
    <field name="arch" type="xml">
      <form>
        <sheet>
          <group>
            <field name="name"/>
            <field name="partner_id"/>
            <field name="email" readonly="1"/>
            <field name="phone" readonly="1"/>
          </group>
          <group>
            <field name="category_id"/>
            <field name="service_id"/>
            <field name="team_id"/>
            <field name="priority"/>
            <field name="stage_id"/>
            <field name="sla_deadline"/>
          </group>
          <notebook>
            <page string="Descripción">
              <field name="description"/>
            </page>
            <page string="Archivos Adjuntos">
              <field name="attachment_ids" widget="many2many_binary"/>
            </page>
            <page string="Chatter">
              <field name="message_follower_ids" widget="mail_followers"/>
              <field name="message_ids" widget="mail_thread"/>
            </page>
            <button name="action_set_done" type="object" string="Marcar como Resuelto"
        groups="helpdesk_web_form.group_helpdesk_manager"
        states="in_progress,pending"/>

<button name="action_escalate" type="object" string="Escalar Urgente"
        attrs="{'invisible': [('priority','!=','3')]}"/>

          </notebook>
        </sheet>
      </form>
    </field>
  </record>

  <record id="action_helpdesk_ticket" model="ir.actions.act_window">
    <field name="name">Tickets</field>
    <field name="res_model">helpdesk.ticket</field>
    <field name="view_mode">tree,form</field>
    <field name="help" type="html">
      <p class="o_view_nocontent_smiling_face">
        Crea tu primer ticket de soporte
      </p>
    </field>
  </record>
</odoo>
